export const styles = {
  wrapper: (theme) => ({
    alignItems: 'center',
    paddingLeft: '8px',
    paddingRight: '8px',
    width: '100%',
    borderRadius: '3px',
    display: 'flex',
    height: 48,
    backgroundColor: theme.basic.panelEventBackgroundColor,
  }),
  buttonContainer: {
    marginLeft: '3px',
    marginRight: '3px',
  },
};
